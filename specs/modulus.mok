# Description: Calculate modulus of two integer literals

function main();
    y := 12 % 5
    x := 6 % 6
    
    write(to := STDOUT, y) # 2
    write(to := STDOUT, x) # 0
    
    from std.io import write, STDOUT
